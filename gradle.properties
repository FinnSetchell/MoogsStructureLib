# Gradle
org.gradle.jvmargs=-Xmx4G
org.gradle.daemon=false

# Project
mod_version=3.0.0
minecraft_version_range=1.20.5-1.20.6
maven_group=com.finndog.moogs_structures
mod_name=Moog's Structure Lib
mod_author=FinnDog
archives_base_name=moogs_structures

# Common
minecraft_version=1.20.6
enabled_platforms=fabric,neoforge

# Forge
neoforge_version=20.6.75-beta

# Fabric
fabric_loader_version=0.15.10
fabric_api_version=0.98.0+1.20.6

# Alpha publish mode.
# When true: CurseForge + Modrinth uploads are marked as ALPHA release type
# and the jar archivesBaseName picks up an -alpha suffix so testers can spot
# the difference at a glance. This branch has no Discord webhook or GitHub
# release CI, so those aren't wired here (add later if needed).
alphaBuild=false
